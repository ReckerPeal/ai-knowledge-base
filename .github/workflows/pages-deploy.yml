name: Deploy Pages

on:
  push:
    branches: [main]
    paths:
      - 'knowledge/articles/**'
      - 'docs/**'
      - 'scripts/build_site_data.py'
      - '.github/workflows/pages-deploy.yml'
  # GITHUB_TOKEN pushes do not fire push events, so chain off the collect
  # workflow's completion to auto-deploy after daily data lands.
  workflow_run:
    workflows: ["Daily Knowledge Collect"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build site
    # Skip when daily-collect failed; deploying stale data would mask the
    # upstream error and waste a Pages slot.
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build site data (index + by_date + dates)
        run: |
          python scripts/build_site_data.py
          ls -lh docs/data/
          ls -lh docs/data/by_date/ || true

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    name: Deploy to Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
